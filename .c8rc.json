{
  "all": true,
  "src": ["src"],
  "extension": [".ts"],
  "include": ["src/**/*.ts"],
  "exclude": [
    "src/**/*.d.ts",
    "test/**",
    "examples/**",
    "dist/**",
    "scripts/**",
    "rollup.config.mjs"
  ],
  "reporter": ["text", "text-summary", "lcov"],
  "report-dir": "coverage",
  "clean": true,
  "//thresholds": "Regression guard, a couple points below actuals (lines ~95, branches ~81, funcs ~90 with full integration run). Enforced only with --check-coverage (CI), since a full run needs coturn + Chromium.",
  "lines": 91,
  "branches": 77,
  "functions": 86
}

